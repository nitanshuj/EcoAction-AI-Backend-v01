# Core Framework
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Async and HTTP
httpx>=0.25.0
asyncpg>=0.29.0

# Data Processing (Core)
pandas>=2.1.0
numpy>=1.24.0

# Database and Auth
supabase>=2.0.0
psycopg2-binary>=2.9.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# Environment and Config
python-dotenv>=1.0.0
pydantic>=2.4.0
pydantic-settings>=2.0.0

# AI/ML (Essential Only)
openai>=1.3.0
google-generativeai>=0.3.0

# CrewAI Framework
crewai>=0.28.0
crewai-tools>=0.4.0
langchain>=0.1.0
langchain-community>=0.0.10
langchain-google-genai>=1.0.0

# Validation and Security
email-validator>=2.1.0
python-multipart>=0.0.6

# Development Tools
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: Only if specifically needed
# torch>=2.1.0  # Large dependency - only if using local models
# tensorflow>=2.14.0  # Large dependency - only if using TensorFlow models
# scikit-learn>=1.3.0  # Only if using sklearn models
# transformers>=4.35.0  # Only if using Hugging Face models

# Visualization (if needed)
altair>=5.1.0

# Logging
structlog>=23.2.0